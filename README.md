# The Gold Room Backend

This is the backend repository for **The Gold Room** project. Follow the steps below to set up the project locally.

## Getting Started

### Step 1: Clone the repository
Clone the project from GitHub using the following command:

```bash
git clone https://github.com/thegoldroom10-sudo/the-gold-room-backend.git
```

cd the-gold-room-backend
npm install


